# 简化修复方案

## 您的需求

> "如果隐藏起来那么麻烦，那就在页面下方建一个框，把请假学生挪进去，并且可收起。这样好弄吗？需要修改数据库吗"

## 回答

✅ **很好弄！不需要修改数据库！**

## 方案

### 1. 移除隐藏功能
- 移除所有 `extra_data` 字段相关代码
- 移除隐藏学生的功能
- 移除 `is_hidden` 相关逻辑

### 2. 添加请假学生区域
- 在页面下方添加一个可收起的区域
- 显示所有 `status='absent'` 的学生
- 可以点击展开/收起

### 3. 优势
- ✅ 不需要修改数据库
- ✅ 使用已有的 `status` 字段
- ✅ 逻辑更简单清晰
- ✅ 不需要 `extra_data` 字段

## 实现步骤

1. **移除 `extra_data` 字段** - 从模型定义中移除
2. **移除隐藏功能代码** - 删除所有 `is_hidden` 相关逻辑
3. **修改数据获取** - 同时获取活跃和请假学生
4. **添加请假学生区域** - 在页面下方显示，可收起

需要我现在开始实现吗？



