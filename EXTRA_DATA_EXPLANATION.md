# extra_data 字段说明

## 问题

您问："extra_data 是什么，为什么要创建它？之前没有啊"

## 回答

您说得对！`extra_data` 字段之前确实没有。

### 它的用途

`extra_data` 字段是用来存储**"隐藏学生"功能的隐藏状态**的。

具体来说：
- 在课堂页面，每个学生卡片上有一个删除按钮（垃圾桶图标）
- 点击后可以"隐藏"学生（不真正删除，只是不显示）
- 这个隐藏状态需要保存在数据库中
- 之前可能用其他方式存储，现在改为用 `extra_data` 字段存储

### 为什么之前没有

这个字段可能是：
1. **新添加的功能** - "隐藏学生"功能是后来添加的
2. **代码重构** - 之前可能用其他方式存储，现在统一用 `extra_data`

### 当前问题

代码中已经在使用 `extra_data` 字段，但数据库表中还没有这个字段，导致报错。

---

## 解决方案选择

### 选择1：保留功能，添加字段

如果您需要"隐藏学生"功能，只需在数据库添加字段：
```sql
ALTER TABLE course_attendances ADD COLUMN IF NOT EXISTS extra_data TEXT;
```

### 选择2：移除功能，不添加字段

如果您不需要"隐藏学生"功能，我可以移除所有相关代码，这样就不需要修改数据库了。

---

## 需要您确认

**您是否使用"隐藏学生"功能？**

- 如果您**从来没用过**这个功能 → 我可以移除相关代码
- 如果您**需要使用**这个功能 → 需要添加数据库字段

请告诉我您的选择！



