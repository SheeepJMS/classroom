# 请假学生显示方案 - 详细说明

## 您的需求

> "如果隐藏起来那么麻烦，那就在页面下方建一个框，把请假学生挪进去，并且可收起。这样好弄吗？需要修改数据库吗"

## 回答

✅ **很好弄！不需要修改数据库！**

---

## 方案设计

### 当前问题

1. 代码使用了 `extra_data` 字段来存储隐藏状态
2. 数据库表中没有这个字段，导致报错
3. 隐藏功能过于复杂

### 新方案

**使用已有的 `status='absent'` 字段**，不需要修改数据库！

### 页面布局

```
┌─────────────────────────────────────────────┐
│  Student Management (11)                    │
│  [+ Add]                                    │
├─────────────────────────────────────────────┤
│  [活跃学生卡片网格...]                       │
│  - 只显示 status='active' 的学生            │
│  - 可以参与答题                              │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  ▼ 请假学生 (2)                            │ ← 可点击展开/收起
├─────────────────────────────────────────────┤
│  [请假学生卡片...]                          │ ← 展开后显示
│  - 只显示 status='absent' 的学生            │
│  - 不参与答题                                │
└─────────────────────────────────────────────┘
```

---

## 需要修改的地方

### 1. 后端修改 (app.py)

#### ✅ 已完成
- 移除 `CourseAttendance.extra_data` 字段定义
- 移除数据库迁移代码

#### ⏳ 需要完成
- 修改 `get_classroom_data` 返回请假学生数据
- 移除所有 `is_hidden` 相关代码
- 移除 `/api/hide_student` API（如果存在）

### 2. 前端修改 (templates/classroom.html)

#### ⏳ 需要完成
- 移除删除/隐藏学生按钮
- 移除 `deleteStudent` 函数
- 添加请假学生显示区域（HTML）
- 添加显示请假学生的函数（JavaScript）
- 移除所有 `is_hidden` 相关代码

---

## 需要您确认的细节

### 1. 请假学生区域样式
- [ ] 默认收起（推荐）
- [ ] 默认展开

### 2. 请假学生卡片内容
- [ ] 只显示姓名和头像（简单）
- [ ] 显示姓名、头像、统计（完整）

### 3. 恢复功能
- [ ] 在请假学生区域添加"恢复"按钮
- [ ] 只在班级管理页面恢复（推荐，更简单）

---

## 优势

- ✅ **不需要修改数据库** - 使用已有的 `status` 字段
- ✅ **逻辑更简单** - 不需要复杂的隐藏状态管理
- ✅ **维护更容易** - 代码更清晰

---

## 待确认

请告诉我：
1. 请假学生区域是否默认收起？
2. 请假学生卡片显示哪些内容？
3. 是否需要恢复按钮？

确认后，我会继续实现！



