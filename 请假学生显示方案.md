# 请假学生显示方案 - 等待确认

## 您的需求

> "如果隐藏起来那么麻烦，那就在页面下方建一个框，把请假学生挪进去，并且可收起。这样好弄吗？需要修改数据库吗"

## 回答

✅ **很好弄！不需要修改数据库！**

## 方案设计

### 1. 页面布局
- **上方**：活跃学生（status='active'）- 参与答题
- **下方**：请假学生区域（可收起）
  - 显示所有 status='absent' 的学生
  - 点击标题栏可以展开/收起
  - 请假学生只显示，不参与答题

### 2. 不需要修改数据库
- ✅ 使用已有的 `status` 字段（'active' 或 'absent'）
- ✅ 不需要 `extra_data` 字段
- ✅ 不需要隐藏功能

### 3. 需要修改的代码

#### 后端 (app.py)
- ✅ 移除 `CourseAttendance.extra_data` 字段（已完成）
- ✅ 移除 `/api/hide_student` API（已完成）
- ✅ 修改 `get_classroom_data` 同时返回活跃和请假学生（已完成）
- ⏳ 移除所有 `is_hidden` 相关代码

#### 前端 (templates/classroom.html)
- ⏳ 移除删除/隐藏学生按钮
- ⏳ 移除 `deleteStudent` 函数
- ⏳ 添加请假学生显示区域（HTML）
- ⏳ 添加显示请假学生的函数（JavaScript）

## 视觉效果

```
┌─────────────────────────────────┐
│  Student Management (11)        │
│  [活跃学生卡片...]               │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  ▼ 请假学生 (2)                │ ← 可点击收起
└─────────────────────────────────┘
  [请假学生卡片...]
```

## 需要您确认

1. **请假学生区域的样式**：
   - [ ] 使用黄色/警告色背景（建议）
   - [ ] 使用灰色背景
   - [ ] 其他颜色

2. **请假学生卡片显示内容**：
   - [ ] 只显示姓名和头像（简单）
   - [ ] 显示姓名、头像、统计数据（完整）

3. **是否需要恢复功能**：
   - [ ] 在请假学生区域添加"恢复"按钮
   - [ ] 不需要，只在班级管理页面恢复

请确认后，我会继续实现！



