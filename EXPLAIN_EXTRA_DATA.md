# 关于 extra_data 字段的说明

## 您的问题

> "extra_data 是什么，为什么要创建它？之前没有啊"

## 回答

您说得对！`extra_data` 字段之前确实没有。

### 它的用途

`extra_data` 字段是用来支持**"隐藏学生"**功能的。这个功能允许在课堂中临时隐藏某些学生（不真正删除），只是不显示在界面上。

### 代码中的使用情况

在代码中，`extra_data` 字段存储 JSON 格式的数据：
```json
{
  "is_hidden": true/false
}
```

这个功能在以下地方使用：
1. `/api/hide_student` API - 隐藏/显示学生
2. `get_classroom_data` - 读取隐藏状态，过滤隐藏的学生
3. `judge_answers` - 评判答案时排除隐藏的学生
4. `next_round` - 下一轮时保持隐藏状态

### 问题所在

代码中已经使用了 `extra_data` 字段，但数据库表中还没有这个字段，所以会报错。

---

## 两个解决方案

### 方案1：添加字段（如果需要隐藏学生功能）

如果您需要"隐藏学生"功能，那么需要添加这个字段。

**最小化操作：**
只需在数据库中运行一条 SQL：
```sql
ALTER TABLE course_attendances ADD COLUMN IF NOT EXISTS extra_data TEXT;
```

### 方案2：移除隐藏功能（如果不需要）

如果您不需要"隐藏学生"功能，我可以：
1. 移除所有与 `extra_data` 和 `is_hidden` 相关的代码
2. 移除模型定义中的 `extra_data` 字段
3. 移除前端的隐藏学生按钮和逻辑

这样就不需要修改数据库了。

---

## 需要您确认

**您是否需要"隐藏学生"功能？**

- [ ] **需要** - 保留功能，添加数据库字段
- [ ] **不需要** - 移除所有相关代码，不修改数据库

请告诉我您的选择，我会相应地调整修复方案。



